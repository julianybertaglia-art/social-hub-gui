services:
  whatsapp-bridge:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    volumes:
      - whatsapp_bridge_data:/data
    restart: unless-stopped

volumes:
  whatsapp_bridge_data:
