name: Executar lote de áudios classificados uma vez

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-batch-20260915-a7f3.yml'

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar lote idempotente
        run: |
          curl --fail-with-body --silent --show-error \
            'https://social-hub-gui.vercel.app/api/whatsapp/batch-20260915-a7f3'
