name: Enviar áudio Kaua uma vez

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/send-kaua-once.yml'

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar envio único protegido
        run: |
          curl --fail-with-body --silent --show-error \
            'https://social-hub-gui.vercel.app/api/whatsapp/send-kaua-once?run=U6MNWCuZ20PWxFYwYgnR1ulz65SwDz13'
